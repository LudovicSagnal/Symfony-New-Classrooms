{% block css %}
    <link rel="stylesheet" href="{{ asset('css/card.css') }}"/>
{% endblock %}

<section class="card">
    <div class="card-left">
        <img src="" alt="">
    </div>
    <div class="card-right">
     <a href="{{ path("app_cours") }}">Cours !!</a>
        <h4>Cours</h4>
        <h3>Titre</h3>
        <div class="level">
            <img src="{{ asset('imgs/level.svg') }}" alt="">
            <h5>Niveau</h5>
            <img src="{{ asset('imgs/clock.svg') }}" alt="">
            <h5>Temps</h5>
        </div>
        <p>Synopsis</p>
    </div>
</section>