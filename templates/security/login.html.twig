{% extends 'base.html.twig' %}

{% block title %}Connexion{% endblock %}
{% block css %}
    <link rel="stylesheet" href="{{ asset('css/style.css') }}"/>
{% endblock %}

{% block body %}
<main>
    <form method="post">
        {% if error %}
            <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
        {% endif %}

        {% if app.user %}
            <div class="mb-3 connect">
                Vous êtes connecté en tant que {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Se déconnecter</a>
            </div>
        {% endif %}

        <h1 class="h3 mb-3 font-weight-normal">ADRAR Classrooms</h1>
        <h2>Bienvenue</h2>
        <div class="div-input">
            <label for="inputEmail">Email</label>
            <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus>
            <label for="inputPassword">Mot de passe</label>
            <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>

            <input type="hidden" name="_csrf_token"
                value="{{ csrf_token('authenticate') }}"
            >

            {#
                Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
                See https://symfony.com/doc/current/security/remember_me.html

                <div class="checkbox mb-3">
                    <label>
                        <input type="checkbox" name="_remember_me"> Remember me
                    </label>
                </div>
            #}

            <button class="btn btn-lg btn-primary" type="submit">
                Se connecter
            </button>
        </div>
        <div class="continu">
            <div class="line"></div>
            <span>ou continuer avec</span>
            <div class="line"></div>
        </div>



    </form>
    <div class="right-div">
        <p>
            « J'étais musicien indépendant, mais la COVID a tout stoppé. On m'a parlé d'ADRAR Classrooms, ça m'a ouvert des opportunités professionnelles insoupçonnées. »
        </p>
        <h3>Alexandre - Développeur Full-Stack Junior</h3>
        <img src="{{ asset('imgs/visage.svg') }}" />
    </div>

</main>
{% endblock %}

